const Visibility = (e) => {
  const button = document
    .querySelectorAll(".badge-btn")[1]
    .classList.toggle("hideshow");
  console.log(button);
  // button.nodelist(1).classList.toggle("hideshow");
};
